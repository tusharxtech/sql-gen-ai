# sql-gen-ai
This project is an AI-powered Text-to-SQL Query Generator built using Streamlit, LangChain, and Ollama (LLaMA 3.2). It allows users to input natural language queries and automatically converts them into executable SQLite SQL queries, then runs them on a local database and displays the results.
